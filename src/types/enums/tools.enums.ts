
export enum ToolId {
  Qibla = "qibla",
  Dhikr = "dhikr",
  Zakat = "zakat",
}

export enum CompassStatus {
  Idle = "idle",
  Active = "active",
  Denied = "denied",
  Unsupported = "unsupported",
}

export enum NisabBasis {
  Gold = "gold",
  Silver = "silver",
}
